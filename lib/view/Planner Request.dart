import 'package:flutter/material.dart';

class Planner_Request extends StatelessWidget {
  const Planner_Request({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
